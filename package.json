{
  "name": "@parsed-path/monorepo",
  "private": true,
  "author": "tsei",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": ["packages/*", "webpage"],
  "scripts": {
    "codecov": "lerna exec -- codecov",
    "update": "lerna exec -- yarn publish",
    "build": "lerna exec -- rollup -c",
    "watch": "lerna exec -- jest --watch --runInBand",
    "test": "lerna exec -- jest --coverage",
    "ls": "lerna exec -- npm pack --dry-run"
  }
}

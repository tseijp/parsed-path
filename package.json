{
  "name": "@parsed-path/monorepo",
  "private": true,
  "author": "tsei",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "docs": "lerna exec --scope @parsed-path/docs yarn start",
    "update": "lerna exec --scope parsed-path -- yarn publish",
    "build": "lerna exec --scope parsed-path -- rollup -c",
    "watch": "lerna exec --scope parsed-path -- jest --watch --runInBand",
    "cov": "lerna exec --scope parsed-path -- jest --coverage",
    "ls": "lerna exec --scope parsed-path -- npm pack --dry-run"
  }
}
